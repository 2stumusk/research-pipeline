# Research Pipeline 完整使用流程

⚠️ **安全提示**: 本文件曾包含真实 API 密钥，已被清理。如果您在 2026-09-01 之前获得了此文件，请立即轮换您的 API 密钥。

## 📋 使用流程

### 1. 准备 PDF 研报

创建日期目录并放入 PDF 文件：

```bash
# 创建今天的目录（格式：YYYY-MM-DD）
mkdir -p inbox/2026-09-01

# 将研报 PDF 放入目录
cp ~/Downloads/*.pdf inbox/2026-09-01/
```

或者直接在 Finder 中：
1. 打开 `inbox` 文件夹
2. 创建日期文件夹（如 `2026-09-01`）
3. 拖入 PDF 文件

---

### 2. 配置 API（首次使用）

设置 Claude API 密钥：

```bash
export ANTHROPIC_API_KEY="sk-ant-xxxxx"  # 替换为您的真实密钥
export ANTHROPIC_BASE_URL="https://api.anthropic.com"  # 可选，使用自定义端点
```

**建议**：将这两行添加到 `~/.zshrc` 或 `~/.bash_profile`，永久生效：

```bash
echo 'export ANTHROPIC_API_KEY="sk-ant-xxxxx"' >> ~/.zshrc  # 替换为您的真实密钥
echo 'export ANTHROPIC_BASE_URL="https://api.anthropic.com"' >> ~/.zshrc
source ~/.zshrc
```

---

### 3. 运行分析

有两种运行方式：

#### 方式 A：命令行运行

```bash
cd /Users/musk2/Desktop/产业链分析/自动化分析项目-prompt/research_pipeline
source .venv/bin/activate

# 运行指定日期的分析
./run.sh 0900  # 会自动使用今天日期

# 或指定日期
python3 -m research_pipeline run --date 2026-09-01 --session 0900
```

#### 方式 B：使用 Demo（测试）

```bash
python3 -m research_pipeline demo
```

这会使用内置的示例 PDF 文件。

---

### 4. 查看结果

运行完成后，结果保存在 `outputs/` 目录：

```bash
# 打开 Dashboard
open outputs/daily/2026-09-01-0900/dashboard.html

# 或查看最新结果
open outputs/demo/dashboard.html
```

**输出文件说明**：

```
outputs/daily/2026-09-01-0900/
├── dashboard.html              # 可视化报告（推荐）
├── 00-今日研报一页纸.md         # 执行摘要
├── 01-今日必读Top10.md          # Top 10 研报详细分析
├── 02-主题共识与分歧.md         # 观点对比
├── 03-全量研报索引.csv          # Excel 可打开
├── 04-风险与催化跟踪.md         # 风险追踪
├── 05-质量检查.md               # QC 报告
├── deep_dive/                  # 每份研报的深度分析
│   ├── report_001.md
│   └── report_002.md
└── machine/                    # 机器可读格式（JSON）
    ├── triage.json
    ├── clusters.json
    └── ...
```

---

## 🔄 完整工作流程示例

### 场景：分析今天收到的研报

```bash
# 1. 创建目录
mkdir -p inbox/2026-09-01

# 2. 放入 PDF（在 Finder 中拖拽）
# inbox/2026-09-01/
#   ├── 中信证券-行业深度.pdf
#   ├── 国泰君安-公司点评.pdf
#   └── 华泰证券-策略周报.pdf

# 3. 激活环境
cd /Users/musk2/Desktop/产业链分析/自动化分析项目-prompt/research_pipeline
source .venv/bin/activate

# 4. 运行分析
./run.sh 0900

# 5. 等待完成（显示进度）
# ✓ 提取文本 (3/3)
# ✓ 初筛评分 (3/3)
# ✓ 事件聚类 (3个簇)
# ✓ 深度分析 (2份)
# ✓ 生成报告

# 6. 自动打开结果
# Dashboard 会自动在浏览器中打开
```

---

## ⚙️ 配置选项

### 修改分析参数

编辑 `config/config.v0.2.yaml`：

```yaml
pipeline:
  # 深度分析数量
  deep_dive_count: 10  # 改成你想要的数量
  
  # 评分阈值
  triage_threshold: 50  # 低于此分数的研报会被过滤
```

### 修改 LLM 配置

```yaml
llm:
  provider: "claude"
  model: "claude-sonnet-5"
  
  # 调整温度参数（创造性）
  temperature:
    triage: 0.0      # 评分阶段（确定性）
    synthesis: 0.0   # 总结阶段
    deep_dive: 0.1   # 深度分析（稍微创造性）
    qc: 0.0          # 质检阶段
```

---

## 🎯 使用技巧

### 1. 批量分析多天研报

```bash
# 准备多天的研报
mkdir -p inbox/2026-09-{01,02,03}
# 分别放入 PDF

# 逐天运行
for date in 2026-09-{01,02,03}; do
    python3 -m research_pipeline run --date $date --session 0900
done
```

### 2. 只看 Top 5

修改配置：
```yaml
pipeline:
  deep_dive_count: 5
```

### 3. 导出到 Excel

CSV 文件可以直接用 Excel 打开：
```bash
open outputs/daily/2026-09-01-0900/03-全量研报索引.csv
```

---

## 💰 成本估算

每份研报大约消耗：
- **初筛评分**: ~2,000 tokens (输入) + 500 tokens (输出)
- **深度分析**: ~8,000 tokens (输入) + 2,000 tokens (输出)

假设 10 份研报，深度分析 Top 3：
- 初筛：10 × 2,500 tokens = 25,000 tokens
- 深度：3 × 10,000 tokens = 30,000 tokens
- **总计**: ~55,000 tokens ≈ $0.15-0.30 USD

---

## 🐛 常见问题

### Q: PDF 无法提取文本？
**A**: 确保 PDF 不是扫描版（图片格式），需要文字可复制的 PDF。

### Q: 分析结果是固定的？
**A**: 如果使用 Mock 模式会生成模拟数据。确保设置了真实 API Key：
```bash
export ANTHROPIC_API_KEY="你的真实Key"
```

### Q: 提示找不到 PDF？
**A**: 检查目录结构：
```
inbox/
  └── 2026-09-01/
      ├── report1.pdf
      └── report2.pdf
```

### Q: API 调用失败？
**A**: 检查：
1. API Key 是否正确
2. Base URL 是否可访问：`curl https://cc-vibe.com`
3. 账户余额是否充足

---

## 📱 快捷方式

创建快捷命令（添加到 `~/.zshrc`）：

```bash
# 快速运行今日分析
alias rp-run='cd /Users/musk2/Desktop/产业链分析/自动化分析项目-prompt/research_pipeline && source .venv/bin/activate && ./run.sh 0900'

# 快速查看结果
alias rp-view='open /Users/musk2/Desktop/产业链分析/自动化分析项目-prompt/research_pipeline/outputs/daily/$(date +%Y-%m-%d)-0900/dashboard.html'
```

使用：
```bash
rp-run    # 运行分析
rp-view   # 查看结果
```

---

## 🎓 学习路径

1. **第一次使用**：运行 `python3 -m research_pipeline demo` 看效果
2. **测试真实数据**：放 1-2 份 PDF 到 `inbox/` 运行
3. **日常使用**：每天早上放入研报，运行分析
4. **高级定制**：修改 `config.yaml` 调整参数

---

需要帮助？查看 [QUICKSTART.md](QUICKSTART.md) 或文档目录。
